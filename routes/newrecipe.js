var express = require('express');
var router = express.Router();

/* GET home page. */
// router.get('/newrecipe', function(req, res, next) {
//   res.render('newrecipe', { title: 'Add A New Recipe' });
// });

module.exports = router;
